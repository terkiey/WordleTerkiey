<Window x:Class="AppWPF.WordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppWPF"
        mc:Ignorable="d"
        Title="Enter 5 Letter Word" Height="150" Width="300"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    
    <StackPanel Margin="10">
        <TextBlock Text="Enter your value:" />
        <TextBox x:Name="UserInputTextBox" Margin="0,5,0,10" Text="{Binding UserInput, UpdateSourceTrigger=PropertyChanged}"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="75" Margin="0,0,5,0" Command="{Binding OkCommand}" IsDefault="True" />
            <Button Content="Cancel" Width="75" Command="{Binding CancelCommand}" />
        </StackPanel>
    </StackPanel>

    <Window.InputBindings>
        <KeyBinding Key="Esc"
                    Command="{Binding CancelCommand}"/>
    </Window.InputBindings>
</Window>
